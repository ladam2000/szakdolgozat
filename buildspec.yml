version: 0.2

phases:
  pre_build:
    commands:
      - echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com
      - REPOSITORY_URI=$AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME
      - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
      - IMAGE_TAG=${COMMIT_HASH:=latest}
  
  build:
    commands:
      - echo "Building AgentCore Docker image..."
      - cd agentcore
      - docker build -t $REPOSITORY_URI:latest .
      - docker tag $REPOSITORY_URI:latest
      - cd ..
  
  post_build:
    commands:
      - echo "Pushing Docker image to ECR..."
      - docker push $REPOSITORY_URI:latest
      - docker push $REPOSITORY_URI:$IMAGE_TAG
      - echo "Build completed successfully"
      - echo ""
      - echo "=========================================="
      - echo "Docker Image URI - $REPOSITORY_URI:latest"
      - echo "=========================================="
      - echo ""
      - echo "Next steps - 1. Go to Amazon Bedrock console"
      - echo "2. Create a new Agent with AgentCore"
      - echo "3. Use the image URI above"
      - echo "4. Update Lambda environment variables with Agent ID and Alias ID"

cache:
  paths:
    - '/root/.cache/pip/**/*'
